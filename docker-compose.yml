services:

  # ── CLIENTS ──────────────────────────────────────────────────────────────
  clients-service:
    image: keahubinnovation/clients:latest
    environment:
      DB_HOST: "srv1078.hstgr.io"
      DB_PORT: "3306"
      DB_NAME: "u549746795_kealabs"
      DB_USER: "u549746795_kealabs"
      DB_PASSWORD: "Sally2025@!"
      JWT_SECRET: "your-secret-key-change-in-production"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.keahubinnovation-clients.rule=(Host(`srv1023256.hstgr.cloud`) || Host(`kealabs.cloud`) || Host(`www.kealabs.cloud`)) && PathPrefix(`/api/clients`)"
      - "traefik.http.routers.keahubinnovation-clients.entrypoints=https"
      - "traefik.http.routers.keahubinnovation-clients.tls=true"
      - "traefik.http.middlewares.keahubinnovation-strip-clients.stripprefix.prefixes=/api"
      - "traefik.http.middlewares.keahubinnovation-cors-clients.headers.accesscontrolalloworiginlist=https://kealabs.cloud,https://www.kealabs.cloud,https://srv1023256.hstgr.cloud"
      - "traefik.http.middlewares.keahubinnovation-cors-clients.headers.accesscontrolallowmethods=GET,POST,OPTIONS"
      - "traefik.http.middlewares.keahubinnovation-cors-clients.headers.accesscontrolallowheaders=Content-Type,Authorization,Accept"
      - "traefik.http.middlewares.keahubinnovation-cors-clients.headers.accesscontrolmaxage=86400"
      - "traefik.http.middlewares.keahubinnovation-cors-clients.headers.addvaryheader=true"
      - "traefik.http.routers.keahubinnovation-clients.middlewares=keahubinnovation-strip-clients,keahubinnovation-cors-clients"
      - "traefik.http.services.keahubinnovation-clients.loadbalancer.server.port=8000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - easypanel

  # ── QUOTES ───────────────────────────────────────────────────────────────
  quotes-service:
    image: keahubinnovation/quotes:latest
    environment:
      DB_HOST: "srv1078.hstgr.io"
      DB_PORT: "3306"
      DB_NAME: "u549746795_kealabs"
      DB_USER: "u549746795_kealabs"
      DB_PASSWORD: "Sally2025@!"
      JWT_SECRET: "your-secret-key-change-in-production"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.keahubinnovation-quotes.rule=(Host(`srv1023256.hstgr.cloud`) || Host(`kealabs.cloud`) || Host(`www.kealabs.cloud`)) && PathPrefix(`/api/quotes`)"
      - "traefik.http.routers.keahubinnovation-quotes.entrypoints=https"
      - "traefik.http.routers.keahubinnovation-quotes.tls=true"
      - "traefik.http.middlewares.keahubinnovation-strip-quotes.stripprefix.prefixes=/api"
      - "traefik.http.middlewares.keahubinnovation-cors-quotes.headers.accesscontrolalloworiginlist=https://kealabs.cloud,https://www.kealabs.cloud,https://srv1023256.hstgr.cloud"
      - "traefik.http.middlewares.keahubinnovation-cors-quotes.headers.accesscontrolallowmethods=GET,POST,OPTIONS"
      - "traefik.http.middlewares.keahubinnovation-cors-quotes.headers.accesscontrolallowheaders=Content-Type,Authorization,Accept"
      - "traefik.http.middlewares.keahubinnovation-cors-quotes.headers.accesscontrolmaxage=86400"
      - "traefik.http.middlewares.keahubinnovation-cors-quotes.headers.addvaryheader=true"
      - "traefik.http.routers.keahubinnovation-quotes.middlewares=keahubinnovation-strip-quotes,keahubinnovation-cors-quotes"
      - "traefik.http.services.keahubinnovation-quotes.loadbalancer.server.port=8000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - easypanel

  # ── CHAT ─────────────────────────────────────────────────────────────────
  chat-service:
    image: keahubinnovation/chat:latest
    environment:
      DB_HOST: "srv1078.hstgr.io"
      DB_PORT: "3306"
      DB_NAME: "u549746795_kealabs"
      DB_USER: "u549746795_kealabs"
      DB_PASSWORD: "Sally2025@!"
      JWT_SECRET: "your-secret-key-change-in-production"
      GEMINI_API_KEY: "AIzaSyBWJ6kGFjXvtC6MfjwlBHihR2tDXHRFXNM"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.keahubinnovation-chat.rule=(Host(`srv1023256.hstgr.cloud`) || Host(`kealabs.cloud`) || Host(`www.kealabs.cloud`)) && PathPrefix(`/api/chat`)"
      - "traefik.http.routers.keahubinnovation-chat.entrypoints=https"
      - "traefik.http.routers.keahubinnovation-chat.tls=true"
      - "traefik.http.middlewares.keahubinnovation-strip-chat.stripprefix.prefixes=/api"
      - "traefik.http.middlewares.keahubinnovation-cors-chat.headers.accesscontrolalloworiginlist=https://kealabs.cloud,https://www.kealabs.cloud,https://srv1023256.hstgr.cloud"
      - "traefik.http.middlewares.keahubinnovation-cors-chat.headers.accesscontrolallowmethods=GET,POST,OPTIONS"
      - "traefik.http.middlewares.keahubinnovation-cors-chat.headers.accesscontrolallowheaders=Content-Type,Authorization,Accept"
      - "traefik.http.middlewares.keahubinnovation-cors-chat.headers.accesscontrolmaxage=86400"
      - "traefik.http.middlewares.keahubinnovation-cors-chat.headers.addvaryheader=true"
      - "traefik.http.routers.keahubinnovation-chat.middlewares=keahubinnovation-strip-chat,keahubinnovation-cors-chat"
      - "traefik.http.services.keahubinnovation-chat.loadbalancer.server.port=8000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - easypanel

  # ── SETTINGS ─────────────────────────────────────────────────────────────
  settings-service:
    image: keahubinnovation/settings:latest
    environment:
      DB_HOST: "srv1078.hstgr.io"
      DB_PORT: "3306"
      DB_NAME: "u549746795_kealabs"
      DB_USER: "u549746795_kealabs"
      DB_PASSWORD: "Sally2025@!"
      JWT_SECRET: "your-secret-key-change-in-production"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.keahubinnovation-settings.rule=(Host(`srv1023256.hstgr.cloud`) || Host(`kealabs.cloud`) || Host(`www.kealabs.cloud`)) && PathPrefix(`/api/settings`)"
      - "traefik.http.routers.keahubinnovation-settings.entrypoints=https"
      - "traefik.http.routers.keahubinnovation-settings.tls=true"
      - "traefik.http.middlewares.keahubinnovation-strip-settings.stripprefix.prefixes=/api"
      - "traefik.http.middlewares.keahubinnovation-cors-settings.headers.accesscontrolalloworiginlist=https://kealabs.cloud,https://www.kealabs.cloud,https://srv1023256.hstgr.cloud"
      - "traefik.http.middlewares.keahubinnovation-cors-settings.headers.accesscontrolallowmethods=GET,POST,OPTIONS"
      - "traefik.http.middlewares.keahubinnovation-cors-settings.headers.accesscontrolallowheaders=Content-Type,Authorization,Accept"
      - "traefik.http.middlewares.keahubinnovation-cors-settings.headers.accesscontrolmaxage=86400"
      - "traefik.http.middlewares.keahubinnovation-cors-settings.headers.addvaryheader=true"
      - "traefik.http.routers.keahubinnovation-settings.middlewares=keahubinnovation-strip-settings,keahubinnovation-cors-settings"
      - "traefik.http.services.keahubinnovation-settings.loadbalancer.server.port=8000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - easypanel

  # ── AGENTS ───────────────────────────────────────────────────────────────
  agents-service:
    image: keahubinnovation/agents:latest
    environment:
      DB_HOST: "srv1078.hstgr.io"
      DB_PORT: "3306"
      DB_NAME: "u549746795_kealabs"
      DB_USER: "u549746795_kealabs"
      DB_PASSWORD: "Sally2025@!"
      JWT_SECRET: "your-secret-key-change-in-production"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.keahubinnovation-agents.rule=(Host(`srv1023256.hstgr.cloud`) || Host(`kealabs.cloud`) || Host(`www.kealabs.cloud`)) && PathPrefix(`/api/agents`)"
      - "traefik.http.routers.keahubinnovation-agents.entrypoints=https"
      - "traefik.http.routers.keahubinnovation-agents.tls=true"
      - "traefik.http.middlewares.keahubinnovation-strip-agents.stripprefix.prefixes=/api"
      - "traefik.http.middlewares.keahubinnovation-cors-agents.headers.accesscontrolalloworiginlist=https://kealabs.cloud,https://www.kealabs.cloud,https://srv1023256.hstgr.cloud"
      - "traefik.http.middlewares.keahubinnovation-cors-agents.headers.accesscontrolallowmethods=GET,POST,OPTIONS"
      - "traefik.http.middlewares.keahubinnovation-cors-agents.headers.accesscontrolallowheaders=Content-Type,Authorization,Accept"
      - "traefik.http.middlewares.keahubinnovation-cors-agents.headers.accesscontrolmaxage=86400"
      - "traefik.http.middlewares.keahubinnovation-cors-agents.headers.addvaryheader=true"
      - "traefik.http.routers.keahubinnovation-agents.middlewares=keahubinnovation-strip-agents,keahubinnovation-cors-agents"
      - "traefik.http.services.keahubinnovation-agents.loadbalancer.server.port=8000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - easypanel

  # ── AUTH ───────────────────────────────────────────────────────────────────
  auth-service:
    image: keahubinnovation/auth:latest
    environment:
      DB_HOST: "srv1078.hstgr.io"
      DB_PORT: "3306"
      DB_NAME: "u549746795_kealabs"
      DB_USER: "u549746795_kealabs"
      DB_PASSWORD: "Sally2025@!"
      JWT_SECRET: "your-secret-key-change-in-production"
      JWT_EXPIRY_HOURS: "8"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.keahubinnovation-auth.rule=(Host(`srv1023256.hstgr.cloud`) || Host(`kealabs.cloud`) || Host(`www.kealabs.cloud`)) && PathPrefix(`/api/auth`)"
      - "traefik.http.routers.keahubinnovation-auth.entrypoints=https"
      - "traefik.http.routers.keahubinnovation-auth.tls=true"
      - "traefik.http.middlewares.keahubinnovation-strip-auth.stripprefix.prefixes=/api"
      - "traefik.http.middlewares.keahubinnovation-cors-auth.headers.accesscontrolalloworiginlist=https://kealabs.cloud,https://www.kealabs.cloud,https://srv1023256.hstgr.cloud"
      - "traefik.http.middlewares.keahubinnovation-cors-auth.headers.accesscontrolallowmethods=GET,POST,OPTIONS"
      - "traefik.http.middlewares.keahubinnovation-cors-auth.headers.accesscontrolallowheaders=Content-Type,Authorization,Accept"
      - "traefik.http.middlewares.keahubinnovation-cors-auth.headers.accesscontrolmaxage=86400"
      - "traefik.http.middlewares.keahubinnovation-cors-auth.headers.addvaryheader=true"
      - "traefik.http.routers.keahubinnovation-auth.middlewares=keahubinnovation-strip-auth,keahubinnovation-cors-auth"
      - "traefik.http.services.keahubinnovation-auth.loadbalancer.server.port=8000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - easypanel

  # ── PROSPECTS ────────────────────────────────────────────────────────────
  prospects-service:
    image: keahubinnovation/prospects:latest
    environment:
      DB_HOST: "srv1078.hstgr.io"
      DB_PORT: "3306"
      DB_NAME: "u549746795_kealabs"
      DB_USER: "u549746795_kealabs"
      DB_PASSWORD: "Sally2025@!"
      JWT_SECRET: "your-secret-key-change-in-production"
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.keahubinnovation-prospects.rule=(Host(`srv1023256.hstgr.cloud`) || Host(`kealabs.cloud`) || Host(`www.kealabs.cloud`)) && PathPrefix(`/api/prospects`)"
      - "traefik.http.routers.keahubinnovation-prospects.entrypoints=https"
      - "traefik.http.routers.keahubinnovation-prospects.tls=true"
      - "traefik.http.middlewares.keahubinnovation-strip-prospects.stripprefix.prefixes=/api"
      - "traefik.http.middlewares.keahubinnovation-cors-prospects.headers.accesscontrolalloworiginlist=https://kealabs.cloud,https://www.kealabs.cloud,https://srv1023256.hstgr.cloud"
      - "traefik.http.middlewares.keahubinnovation-cors-prospects.headers.accesscontrolallowmethods=GET,POST,OPTIONS"
      - "traefik.http.middlewares.keahubinnovation-cors-prospects.headers.accesscontrolallowheaders=Content-Type,Authorization,Accept"
      - "traefik.http.middlewares.keahubinnovation-cors-prospects.headers.accesscontrolmaxage=86400"
      - "traefik.http.middlewares.keahubinnovation-cors-prospects.headers.addvaryheader=true"
      - "traefik.http.routers.keahubinnovation-prospects.middlewares=keahubinnovation-strip-prospects,keahubinnovation-cors-prospects"
      - "traefik.http.services.keahubinnovation-prospects.loadbalancer.server.port=8000"
    deploy:
      replicas: 1
      restart_policy:
        condition: on-failure
    networks:
      - easypanel

networks:
  easypanel:
    external: true
